const initialState = {
  colors: {
    mainColor: '#ffabce',
    secondaryColor: '#ff7bad',
    eyeColor: '#000000',
  },
};

export const profileReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
